/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["lh3.googleusercontent.com", "seeklogo.com", "upload.wikimedia.org", "img.cryptorank.io"]
    }
};

export default nextConfig;
